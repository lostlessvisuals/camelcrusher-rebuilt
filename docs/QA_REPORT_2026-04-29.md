# QA Report

Date: `2026-04-29`

## Ship Recommendation

`ready with caveats`

## Blocking Issues

- None for local/private distribution on this machine.

## Non-Blocking Issues

- The installer is still unsigned and not notarized, so other Macs will see the usual unidentified-developer friction.
- Expanded installer BOMs still contain AppleDouble-style `._*` receipt paths caused by macOS provenance metadata during packaging. This is packaging noise, not a known runtime failure.
- The newest installer package now contains the `VST2` compatibility-alias postinstall fix, but that exact package has not yet been revalidated on a second machine or fresh user account.

## Metadata And Release Tasks

- If publishing under `Lossless Visuals`, the clean default repo name is `camelcrusher-rebuilt`.
- Use `docs/assets/camelcrusher-ui-preview.png` as the main GitHub screenshot.
- Use `docs/PUBLIC_COPY.md` for GitHub description, README text, and release blurb.

## QA Checklist

- `build/` test suite passes: `ctest --test-dir build --output-on-failure`
- `build-release/` release test suite passes: `ctest --test-dir build-release --output-on-failure`
- `tools/build_macos_release.sh` completes successfully and regenerates the installer bundle
- Installed `AU` host app exists at `/Applications/CamelCrusher Host.app`
- Installed `AU` instantiates successfully and reports custom UI support through `modern_au_component_instantiate`
- Installed `VST2` bundle loads from `/Library/Audio/Plug-Ins/VST/CamelCrusher.vst`
- Installed `VST3` bundle loads from `/Library/Audio/Plug-Ins/VST3/CamelCrusher.vst3`
- User manually confirmed old Ableton Live `VST2` project recall works again after restoring the user-path CamelCrusher alias
